#include "Ui.h"

/*  Bearbeitet von Becker, Ibo; Hübner Christian; Pydde Alexander
 *
 * Der Code funktioniert bei uns in Clion problemlos
 *
 * Beim ausführen wird ein geführter Dialog in der Konsole angezeigt.
 */

int main() {
    Ui ui;
    ui.anwendung();

    return 0;
}
